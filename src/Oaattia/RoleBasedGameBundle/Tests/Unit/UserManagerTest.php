<?php

namespace Oaattia\RoleBasedGameBundle\Tests\Unit;

use Doctrine\ORM\EntityManagerInterface;
use Oaattia\RoleBasedGameBundle\DomainManager\UserManager;
use Oaattia\RoleBasedGameBundle\Entity\User;
use PHPUnit\Framework\TestCase;

class UserManagerTest extends TestCase
{
    public function test_perform_attack_to()
    {
//        $user = $this->prophesize(User::class);
//        $currentUser = $this->prophesize(User::class);
//
//
//        $this
//            ->prophesize(UserManager::class)
//            ->restCharactersIfTurnCompleted($user->reveal(), $currentUser->reveal())->shouldBeCalled();
//
//        $userManager = new UserManager($this->prophesize(EntityManagerInterface::class)->reveal());
//        $userManager->performAttackTo($user->reveal(), $currentUser->reveal());
    }
}
